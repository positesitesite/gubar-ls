.scroll {
  display: flex;
  align-items: center;

  background-color: transparent;
  z-index: 10;
}

.scroll__name {
  color: $white;
}

.scroll__icon {
  width: 20px;
  min-height: 100%;
  fill: $white;
}

